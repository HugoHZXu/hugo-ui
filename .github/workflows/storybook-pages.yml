name: Deploy Storybook to GitHub Pages

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: github-pages
  cancel-in-progress: false

jobs:
  build:
    name: Build Storybooks
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Set up Node.js
        uses: actions/setup-node@v6
        with:
          node-version-file: .nvmrc

      - name: Install pnpm
        run: |
          PNPM_VERSION="$(node -p "require('./package.json').packageManager.replace('pnpm@', '')")"
          npm install --global "pnpm@$PNPM_VERSION"

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build React Storybook
        run: pnpm run build-storybook

      - name: Build Vue Storybook
        run: pnpm run build-storybook-vue

      - name: Assemble Pages site
        run: |
          rm -rf pages-site
          mkdir -p pages-site/vue
          cp -R packages/storybook/storybook-static/. pages-site/
          cp -R packages/storybook-vue/storybook-static/. pages-site/vue/
          touch pages-site/.nojekyll

      - name: Configure GitHub Pages
        uses: actions/configure-pages@v6

      - name: Upload Storybooks artifact
        uses: actions/upload-pages-artifact@v5
        with:
          path: pages-site

  deploy:
    name: Deploy to GitHub Pages
    needs: build
    runs-on: ubuntu-latest
    permissions:
      pages: write
      id-token: write
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy Storybooks
        id: deployment
        uses: actions/deploy-pages@v5
