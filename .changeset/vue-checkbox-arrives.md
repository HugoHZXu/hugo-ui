---
'@hugo-ui/shadcn-vue': minor
---

Add a Vue Checkbox component with checked, unchecked, indeterminate, disabled, hover, active, and focus-visible states.

Add an optional resizable DataGrid checkbox selection column with configurable header select-all, indeterminate state, and isolated checkbox events.

Add a DataGrid `endReached` integration point with loading-more state for infinite-scroll demos and external paging logic.
