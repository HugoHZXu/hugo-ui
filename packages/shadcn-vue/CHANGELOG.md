# @hugo-ui/shadcn-vue

## 0.2.0

### Minor Changes

- f8611ba: Add Vue StatusBadge, EmptyState, ErrorState, MetricTile, and DropdownMenu components with tests and Storybook coverage.
- 8cd00ae: Add Vue Progress, WorkflowStepper, Timeline, FileDropzone, and Upload components with tests and Storybook coverage.
- 9718085: Add the initial Vue shadcn package with design tokens, Badge, Button, Card, DataGrid, Input, PageTemplate, ContentTemplate, and Storybook coverage.
- af8311c: Add a Vue Checkbox component with checked, unchecked, indeterminate, disabled, hover, active, and focus-visible states.

  Add an optional resizable DataGrid checkbox selection column with configurable header select-all, indeterminate state, and isolated checkbox events.

  Add a DataGrid `endReached` integration point with loading-more state for infinite-scroll demos and external paging logic.

- c9537f7: Add a Hugo-style Vue Modal component with typed variants, default action buttons, loading states, slot overrides, Storybook examples, and tests.
- Add Vue Select and Combobox components with shared choice option types, tests, Storybook coverage, and local or async search support.
