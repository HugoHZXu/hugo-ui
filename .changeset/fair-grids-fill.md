---
'@hugo-ui/shadcn-vue': minor
---

Add a `fill` prop to DataGrid for parent-height layouts with internal viewport scrolling.
DataGrid now renders every row by default, and long-list virtualization is enabled only when
`virtualized` is set.
